<h1>Entities</h1>

<%= link_to "New entity", new_entity_path %>

<div id="entities">
  <% @entities.each do |entity| %>
    <%= render entity %>
    <p>
      <%= link_to "Show this entity", entity %>
    </p>
  <% end %>
</div>

<%= paginate @entities %>
