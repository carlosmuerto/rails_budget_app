<main class="group-edit">
  <h1>Editing <%= "#{@group.icon} #{@group.name}" %></h1>
  <%= render "form", group: @group %>
  <div class="links">
    <div class="show">
      <%= link_to "Show this group", @group %>
    </div>
  </div>
</main>